import { jsx } from "@emotion/core";
import { FC } from "react";
import Layout from "./Layout";

const NotFound: FC = () => (
  <Layout>
    <div>
      <h1>Not found</h1>
    </div>
  </Layout>
);

export default NotFound;
