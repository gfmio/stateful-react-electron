import { jsx } from "@emotion/core";
import { FC } from "react";
import Layout from "./Layout";

const Home: FC = () => (
  <Layout>
    <div>
      <h1>Hello World</h1>
    </div>
  </Layout>
);

export default Home;
